# Drone-Stolos plugin

```Shell
docker run --rm \
    -e PLUGIN_STOLOS_URL=$PLUGIN_STOLOS_URL \
    -e PLUGIN_USERNAME=$PLUGIN_USERNAME \
    -e PLUGIN_PASSWORD=$PLUGIN_PASSWORD \
    -e PLUGIN_PROJECT_UUID=$PLUGIN_PROJECT_UUID \
    intelligems/drone-stolos:latest \
    bash
```